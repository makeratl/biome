generated-images/
node_modules/
.DS_Store
*.pyc
__pycache__/
tournament_log.json
model-roster.json
*.bak

*.mbtree

CLAUDE.md

# dev-session skill runtime artifacts (rolling logs, canary screenshot, chrome profile)
dev-logs/

.claude/

nohup

# biome.db is a live SQLite DB (ELO source of truth) — local runtime state.
# Tracking it committed WAL-inconsistent bytes that read back as a malformed
# image; db.py rebuilds/migrates it on boot. Keep it (and its sidecars) local.
biome.db

*.db-shm

*.db-wal

videos/

# Training Lab capture (per-turn trajectories, round signals, outcomes, datasets)
training-data/
